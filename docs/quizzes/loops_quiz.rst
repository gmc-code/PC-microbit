====================================================
For Loops Quiz
====================================================

Section 1: Multiple Choice
==========================

Question 1
----------

.. multichoice::

    When iterating over a string like `word = "HELLO"` using a `for` loop, what does the loop variable hold during each step?
    [x] A single character/letter from the string | Correct! The loop works through one letter at a time.
    [ ] The entire word "HELLO" | Incorrect. `word` holds the full string, while the loop variable accesses one letter at a time.
    [ ] The index position of the letter | Incorrect. The loop yields each character directly rather than its index.

----

Question 2
----------

.. multichoice::

    What numbers will `range(5)` generate in Python?
    [ ] 1, 2, 3, 4, 5 | Incorrect. `range()` always starts counting at 0 unless specified otherwise.
    [x] 0, 1, 2, 3, 4 | Correct! `range()` counts starting from 0 and stops right before the given number.
    [ ] 0, 1, 2, 3, 4, 5 | Incorrect. It always stops before reaching the specified limit.

----

Question 3
----------

.. multichoice::

    If a list is defined as `animals = ["Duck", "Rabbit", "Cow"]`, how many times will `for animal in animals:` loop?
    [ ] 1 time | Incorrect. The loop iterates through each item in the list.
    [x] 3 times | Correct! There are 3 items in the list, so the loop runs 3 times.
    [ ] 12 times | Incorrect. It iterates per item in the list, not per letter.

----

Question 4
----------

.. multichoice::

    What output will `display.show(number + 1)` produce inside a `for number in range(5):` loop?
    [ ] 0, 1, 2, 3, 4 | Incorrect. Adding 1 shifts the range from 0..4 to 1..5.
    [x] 1, 2, 3, 4, 5 | Correct! Adding 1 to each generated value (0 through 4) produces numbers 1 to 5.
    [ ] 1, 3, 5, 7, 9 | Incorrect. This would require multiplying by 2 or stepping by 2.

----

Question 5
----------

.. multichoice::

    What sequence of numbers is shown by `display.show(number * 2)` when looping through `range(5)`?
    [ ] 2, 4, 6, 8, 10 | Incorrect. The first number generated by range(5) is 0, so 0 * 2 = 0.
    [x] 0, 2, 4, 6, 8 | Correct! It multiplies 0, 1, 2, 3, and 4 by 2.
    [ ] 0, 1, 2, 3, 4 | Incorrect. Each value is multiplied by 2 before display.

----

Section 2: Cloze
=====================================

Question 6
----------

| Complete the code to loop through each letter in the word "HELLO" and scroll it.

.. cloze::

    from microbit import *

    word = "HELLO"

    while True:
        @@ for | while | repeat @@ letter @@ in | from | inside @@ word:
            display.scroll(letter)

----

Question 7
----------

| Complete the code to display each number from a list of numbers with a pause.

.. cloze::

    from microbit import *

    numbers = [1, 2, 3, 4]

    while True:
        for number @@ in | from | with @@ numbers:
            display.@@ show | scroll | print @@(number)
            sleep(500)

----

Question 8
----------

| Complete the loop using `range()` to count from 0 to 4.

.. cloze::

    from microbit import *

    while True:
        for number in @@ range(5) | range(4) | range(0, 4) @@:
            display.show(number)
            sleep(500)

----

Question 9
----------

| Complete the code to display the numbers 1, 2, 3, 4, 5 using `range()`.

.. cloze::

    from microbit import *

    while True:
        for number in range(5):
            display.show(number @@ + 1 | * 1 | + 0 @@)
            sleep(500)

----

Question 10
-----------

| Complete the code to display multiples of 3: 0, 3, 6, 9.

.. cloze::

    from microbit import *

    while True:
        for number in range(4):
            display.show(number @@ * 3 | + 3 | ** 3 @@)
            sleep(500)

----

Section 3: Code Ordering
========================

Question 11
-----------

| Order the lines to scroll each letter of "Python" continuously.

.. ordering::
    :theme: light

    from microbit import *

    word = "Python"

    while True:
        for letter in word:
            display.scroll(letter)

----

Question 12
-----------

| Put the code segments in order to display each animal in a list.

.. ordering::
    :theme: light

    from microbit import *

    animals = ["Duck", "Rabbit", "Cow"]

    while True:
        for animal in animals:
            display.scroll(animal)

----

Question 13
-----------

| Order the lines to scroll each letter of "CAT" followed by showing a heart image.

.. ordering::
    :theme: light

    from microbit import *

    word = "CAT"

    while True:
        for letter in word:
            display.scroll(letter)
            display.show(Image.HEART)

----

Question 14
-----------

| Order the code to count from 0 up to 6 on the screen.

.. ordering::
    :theme: light

    from microbit import *

    while True:
        for number in range(7):
            display.show(number)
            sleep(500)

----

Question 15
-----------

| Put the code in order to display the sequence: 5, 6, 7, 8.

.. ordering::
    :theme: light

    from microbit import *

    while True:
        for number in range(4):
            display.show(number + 5)
            sleep(500)


